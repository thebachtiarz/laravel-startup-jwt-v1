const encryptPassword = (password) => {
    let encs = forge.md.sha384.create(), encn = forge.md.sha512.create();
    encs.update(password), encn.update(encs.digest().toHex());
    return encn.digest().toHex();
}
